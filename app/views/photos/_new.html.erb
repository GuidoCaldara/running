
  <%= simple_form_for [race, photo], remote: true do |f|%>
  <div class="form-group file required photo_image">
  </div>
  <label class="file required" for="photo_image" id="photo_label">Aggiungi una foto  <i class="fas fa-camera"></i></label>
  <%= f.input :image, label: false %>
  <%= f.input :image_cache, as: :hidden %>
  <% end %>
